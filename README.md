# bunbao

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
node index.mjs
```

## Questions
Why all in mjs?
Bao is bun first framework but still needs to run on nodejs 
